{
  "name": "verve",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "FORCE_COLOR=1 dotenv -- turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "generate:types": "cd apps/cms && pnpm generate:types"
  },
  "devDependencies": {
    "@repo/eslint": "workspace:*",
    "@repo/typescript": "workspace:*",
    "@turbo/gen": "^1.11.3",
    "dotenv-cli": "^7.3.0",
    "prettier": "^3.1.1",
    "prettier-plugin-tailwindcss": "^0.5.11",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}

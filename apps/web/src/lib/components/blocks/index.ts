/**
 * Export all blocks from the blocks/blocks package
 *
 * e.g.:
 * export { Block } from "@repo/blocks/blocks";
 *
 * run `pnpm generate:block` in the root dir to create a new block
 */

export { default as ScrollWheel } from "./scroll-wheel.svelte";

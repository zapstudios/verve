import type { Block } from "payload/types";

export const {{ pascalCase name }}Config: Block = {
  slug: "{{ camelCase name }}",
  labels: {
    singular: "{{ pascalCase name }} singular",
    plural: "{{ pascalCase name }} plural",
  },
  fields: [
    {
      name: "label",
      label: "Label",
      type: "text",
      required: false,
    },
  ],
};
